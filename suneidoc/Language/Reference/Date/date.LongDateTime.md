#### date.LongDateTime

``` suneido
() => string
```

Returns [date.LongDate](<date.LongDate.md>)() $ " " $ [date.Time](<date.Time.md>)().

For example:

``` suneido
Date().LongDateTime()
    => "Sunday, August 21, 2005 11:35"
```