#### date.WeekNumber

``` suneido
(firstday = 'sun') => number
```

Returns which week of the year the date falls into.

For example:

``` suneido
#20050821.WeekNumber()
    => 35
```

See also:
[date.WeekDay](<date.WeekDay.md>)