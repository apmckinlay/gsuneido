### Abs

``` suneido
(number) => number
```

Returns [number.Abs](<Number/number.Abs.md>)(). Useful in queries where you can't call methods.

For example:

``` suneido
Abs(1.23)
    => 1.23

Abs(-1.23)
    => 1.23
```