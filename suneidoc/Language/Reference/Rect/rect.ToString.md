### rect.ToString

``` suneido
( ) => string
```

Returns a string representation for the current rect.

For example:

``` suneido
rect = Rect(50, 100, 10, 20)
Print(rect) 
    => Rect(50, 100, 10, 20)
```