#### popclient.GetMessageSize

``` suneido
(i) => Number or false
```

Gets the size of the message at index i. The return value is in
octets. false is returned if there is no message for the specified index.