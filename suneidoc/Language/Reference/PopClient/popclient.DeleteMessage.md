#### popclient.DeleteMessage

``` suneido
(index) => True or False
```

Deletes the message from the mailbox at the given index.  The index is not zero based, 
message indexes start at 1.   Returns True if successful, False otherwise.