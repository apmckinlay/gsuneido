### Gather Assignment

`@ob = function-call`

Creates an object containing the return values (if any) from the function call.

`ob` must be a local variable name.

The inverse is [return](<return.md>) @ob

(BuiltDate > 2026-09-21)