### StatusTextControl

``` suneido
(text)
```

Display **text** similar to a status bar.

For example:

``` suneido
Window(#(StatusText 'hello world'))
```

will creates

![](<../../res/status_text.gif>)