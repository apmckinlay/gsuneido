### [WndProc](<../WndProc.md>) - Methods
`SubClass()`
: Saves the previous window procedure and then assigns *this* as the new window procedure. Used to handle window messages before they are sent to a control.